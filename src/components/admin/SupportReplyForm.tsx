"use client"
import { useState } from "react"
import { useRouter } from "next/navigation"
export function SupportReplyForm({ id }: { id: string }) { const [body, setBody] = useState(""); const [saving, setSaving] = useState(false); const router = useRouter(); async function send(event: React.FormEvent) { event.preventDefault(); setSaving(true); const res = await fetch(`/api/admin/support/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ body }) }); const data = await res.json().catch(() => ({})); setSaving(false); if (!res.ok) return alert(data.error || "La réponse n’a pas pu être enregistrée."); setBody(""); router.refresh() } return <form onSubmit={send} className="mt-6 rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><label className="text-sm font-bold text-blue-950">Votre réponse</label><textarea required minLength={2} value={body} onChange={e => setBody(e.target.value)} rows={6} placeholder="Écrivez votre réponse au client…" className="mt-2 w-full rounded-xl border border-slate-200 p-3 text-sm outline-none focus:border-blue-700 focus:ring-4 focus:ring-blue-100" /><p className="mt-2 text-xs text-slate-500">La réponse apparaît directement dans le mini-chat du client.</p><button disabled={saving} className="mt-4 rounded-xl bg-blue-950 px-5 py-3 text-sm font-bold text-white hover:bg-blue-800 disabled:opacity-50">{saving ? "Envoi…" : "Envoyer dans le chat"}</button></form> }
