import { prisma } from "@/lib/prisma"; import { EmptyState, PageHeader } from "@/components/admin/AdminUi"
export const dynamic = "force-dynamic"
export default async function ActivityPage() { const events = await prisma.adminActivity.findMany({ include: { actor: { select: { name: true, email: true } } }, orderBy: { createdAt: "desc" }, take: 100 }); return <><PageHeader title="Activité" description="Journal auditable des actions administrateur et événements importants." />{events.length ? <div className="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200">{events.map(e => <div className="border-b py-4 last:border-0" key={e.id}><p className="font-bold">{e.action}</p><p className="text-sm text-slate-500">{e.actor?.name || e.actor?.email || "Système"} · {e.entityType} · {e.createdAt.toLocaleString("fr-FR")}</p></div>)}</div> : <EmptyState>Aucun événement n’a encore été enregistré.</EmptyState>}</> }
