import { prisma } from "@/lib/prisma"; import { EmptyState, PageHeader } from "@/components/admin/AdminUi"
export const dynamic = "force-dynamic"
export default async function NotificationsPage() { const notifications = await prisma.adminNotification.findMany({ orderBy: { createdAt: "desc" }, take: 100 }); return <><PageHeader title="Notifications" description="Alertes administratives reliées aux événements de la boutique." />{notifications.length ? <div className="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200">{notifications.map(n => <div className="border-b py-4 last:border-0" key={n.id}><p className="font-bold">{n.title}</p>{n.body && <p className="text-sm text-slate-500">{n.body}</p>}<p className="mt-1 text-xs text-slate-400">{n.createdAt.toLocaleString("fr-FR")}</p></div>)}</div> : <EmptyState>Aucune notification à afficher.</EmptyState>}</> }
