import Link from "next/link"
import { prisma } from "@/lib/prisma"
import { EmptyState, PageHeader, StatusBadge } from "@/components/admin/AdminUi"
export const dynamic = "force-dynamic"
export default async function SupportPage() { const tickets = await prisma.supportTicket.findMany({ include: { messages: { orderBy: { createdAt: "desc" }, take: 1 } }, orderBy: { updatedAt: "desc" }, take: 100 }); return <><PageHeader title="Support" description="Les messages reçus depuis le formulaire de contact de la boutique." />{tickets.length ? <div className="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-4">Client</th><th>Sujet</th><th>Dernier message</th><th>État</th><th>Date</th><th /></tr></thead><tbody>{tickets.map(ticket => <tr className="border-b last:border-0" key={ticket.id}><td className="p-4"><p className="font-bold">{ticket.name}</p><p className="text-xs text-slate-500">{ticket.email}</p></td><td>{ticket.subject}</td><td className="max-w-[240px] truncate text-slate-500">{ticket.messages[0]?.body || "—"}</td><td><StatusBadge value={ticket.status} /></td><td>{ticket.updatedAt.toLocaleDateString("fr-FR")}</td><td className="p-4"><Link className="font-semibold text-blue-800" href={`/admin/support/${ticket.id}`}>Répondre</Link></td></tr>)}</tbody></table></div> : <EmptyState>Les nouveaux messages du formulaire de contact apparaîtront ici.</EmptyState>}</> }
