import { notFound } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { formatEuro } from "@/lib/admin"; import { PageHeader, EmptyState } from "@/components/admin/AdminUi"
export default async function UserDetail({ params }: PageProps<"/admin/users/[id]">) { const { id } = await params; const user = await prisma.user.findUnique({ where: { id }, include: { orders: { include: { items: true }, orderBy: { createdAt: "desc" } }, carts: { orderBy: { updatedAt: "desc" }, take: 1 } } }); if (!user) notFound(); const paid = user.orders.filter(o => o.paymentStatus === "PAID"); const spent = paid.reduce((sum, o) => sum + o.total, 0); return <><PageHeader title={user.name || "Client"} description={user.email} /><section className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><p className="text-xs text-slate-500">Commandes</p><p className="text-2xl font-black">{user.orders.length}</p></div><div className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><p className="text-xs text-slate-500">Total dépensé</p><p className="text-2xl font-black">{formatEuro(spent)}</p></div><div className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><p className="text-xs text-slate-500">Panier moyen</p><p className="text-2xl font-black">{formatEuro(paid.length ? spent / paid.length : 0)}</p></div></section><section className="mt-6 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 className="font-bold text-blue-950">Historique des commandes</h2>{user.orders.length ? <div className="mt-4 space-y-3">{user.orders.map(o => <div className="flex justify-between border-b pb-3 last:border-0" key={o.id}><span>#{o.id.slice(-8).toUpperCase()} · {o.items.length} article(s)</span><span className="font-bold">{formatEuro(o.total)}</span></div>)}</div> : <EmptyState>Ce client n’a pas encore commandé.</EmptyState>}</section></> }
